services:
  - type: web
    name: ai-worker-dashboard
    env: docker
    plan: free
    region: oregon
    # This maps Render's public URL to your internal port
    envVars:
      - key: PORT
        value: 8000
